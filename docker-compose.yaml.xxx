services:
  webserver:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
    - ./nginx/club-management.conf:/etc/nginx/conf.d/club-management.conf
    - ./app:/app
    
  php:
    image: php:8.1-fpm
    volumes:
      - ./:/app
